name: CI Agent Coordination

on:
  push:
    paths:
      - "agent_coordination_protocol-financial_intelligence/**"
      - ".github/workflows/ci-agent-coordination.yml"
  pull_request:
    paths:
      - "agent_coordination_protocol-financial_intelligence/**"
      - ".github/workflows/ci-agent-coordination.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: agent_coordination_protocol-financial_intelligence
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: agent_coordination_protocol-financial_intelligence/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
